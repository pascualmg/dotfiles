.aider*

# Wallpapers - too large for git
wallpapers/

# Aider cache
.aider.tags.cache.v4/

# Fish shell temporaries
fish/.config/fish/fish_variables
fish/.config/fish/conf.d/fish_frozen_*

# Nix build results
result
result-*

# Claude Code local settings (cada máquina tiene los suyos)
claude-code/.claude/settings.local.json

# Carpetas stow vacías/locales (configs ya están en home-manager)
# alacritty: ignorar todo excepto themes/custom (temas editables)
alacritty/*
!alacritty/themes
alacritty/themes/*
!alacritty/themes/custom
fish/
