# =============================================================================
# Device-specific config: ANDROID (default)
# =============================================================================
# Configuracion especifica para este dispositivo.
# La base comun esta en droid/common.nix
#
# Futuro: Si tienes multiples moviles, crea droid/pixel/, droid/samsung/, etc.
# =============================================================================

{ pkgs, ... }:

{
  # Aqui van overrides especificos del dispositivo
  # Por ahora vacio - todo viene de common.nix
}
